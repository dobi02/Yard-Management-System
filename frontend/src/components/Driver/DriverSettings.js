import React from 'react'
import DriverLayout from "./DriverLayout";

const DriverSettings = () => {
    return (
        <DriverLayout>
            <h2>Settings</h2>
            <p> 설정 (주로 계정) </p>
            <p> 드라이버 개인 설정 변경 (이름? 전화번호 등) 비번 변경 로그아웃 버튼 </p>
        </DriverLayout>
    );
};

export default DriverSettings;